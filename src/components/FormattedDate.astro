---
import dayjs from "dayjs";
import utc from "dayjs/plugin/utc";

dayjs.extend(utc);

interface Props {
	date: Date;
}

const { date } = Astro.props;

let dayjsDate = dayjs(date).utc().startOf("day");

---

<time datetime={dayjsDate.toString()}>
	{dayjsDate.format("D MMM YYYY")}
</time>
